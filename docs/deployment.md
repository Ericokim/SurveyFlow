# Deployment

The app is configured for Netlify.

- Build command: `vite build`
- Publish directory: `dist/client`
- Node version: `22`

Production deployments need environment variables from `.env.example`.
