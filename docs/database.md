# Database

SurveyFlow uses MongoDB through Mongoose.

Initial models:

- Company
- User
- Survey
- Recipient
- Response
- Communication
- AuditLog

Add the database connection at `src/server/db/mongoose.ts` when database work begins.
