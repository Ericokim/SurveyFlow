# Command 04: Review Auth UI

## Goal

Review implementation before final checks.

## Checklist

- Route matches existing auth namespace.
- No duplicate login route.
- Uses TanStack Router `Link`.
- Uses shadcn components.
- Uses theme classes.
- No hardcoded brand hex colors in TSX.
- No external image URLs.
- No Next.js imports.
- No `.env` access.
- Form is UI-only.
- Mobile layout works.
- Desktop layout matches the screenshot.

Fix critical issues before proceeding.
